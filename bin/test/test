# Use this cpp code to test your code

```c
/**
 * @brief ### Set the up io redirection function
 * *
 * - Test app using i/o files (for large i/o data)
 */
void setup_io_redirection() {
#ifndef __TEST__
  freopen("doc/input", "r", stdin);
  freopen("out/output", "w", stdout);
#endif
}
```
